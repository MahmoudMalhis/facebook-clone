import React from "react";
import styles from "./logo.module.css";

const Logo = () => {
  return (
    <img src="./image/logo.svg" alt="Facebook-logo" className={styles.img} />
  );
};

export default Logo;
